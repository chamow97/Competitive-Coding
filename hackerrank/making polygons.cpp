#include<bits/stdc++.h>

using namespace std;

int main()
{
	long long maximum=0,len=0,i,n,a[50];
	cin>>n;
	for(i=0;i<n;i++)
		{
			cin>>a[i];
			len+=(a[i]);
			maximum=max(maximum,a[i]);
		}
		if(n==1)
        {
            cout<<"3\n";
        }
        else if


        }

}
